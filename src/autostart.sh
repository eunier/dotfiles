tree ~/.config/autostart > ~/.dotfiles/src/tree/tree-autostart-after.txt

tree \
  -a \
  -D \
  -g \
  -N \
  -p \
  -q \
  -s \
  -u \
  ~/.config/autostart >> ~/.dotfiles/src/tree/tree-autostart-after.txt